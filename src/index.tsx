import React from 'react';
import ReactDOM from 'react-dom/client';
import EntirePath from './EntirePath';

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement);

root.render(
  <React.StrictMode>
    <EntirePath />
  </React.StrictMode>
);