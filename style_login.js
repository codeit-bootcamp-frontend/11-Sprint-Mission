// 버튼 클릭 효과 //
// 눈 모양 //

// const loginButton = document.querySelector('.login_button');
// const signupButton = document.querySelector('.signup_button');
// const inputTags = document.querySelectorAll('input');

// const checkInputs = () => {
//   const isFilled = [...inputTags].every(input => input.value.trim() !== '');
//   loginButton.classList.toggle('active', isFilled);
//   signupButton.classList.toggle('active', isFilled);
// };

// inputTags.forEach(input => input.addEventListener('input', checkInputs));

// input 관련해 만드는 중

// function loginCheck() {
//   const email = document.getElementsByid("email");
//   const password = document.getElementById("passworrd");

//   if (!email.checkValidity()) {
//     return alert(email.validationMessage);
//   }

//   if (!password.checkValidity()) {
//     return alert(password.validationMessage);
//   }
// }
